# 🎯 AWK - Визуальная шпаргалка

## 📐 Как AWK видит строку

```
Исходная строка:
┌─────────────────────────────────────────────────────────────────┐
│ John    Smith    25    Engineer    New York                     │
└─────────────────────────────────────────────────────────────────┘

AWK разбивает на поля (по пробелам):
┌────────┬────────┬────┬───────────┬──────────┐
│  $1    │  $2    │ $3 │    $4     │   $5     │
│ John   │ Smith  │ 25 │ Engineer  │ New York │
└────────┴────────┴────┴───────────┴──────────┘
     ↑        ↑      ↑       ↑          ↑
   Поле 1  Поле 2  Поле 3  Поле 4   Поле 5

$0 = вся строка целиком
$NF = последнее поле (в данном случае "New York")
NF = количество полей (в данном случае 5)
```

---

## 🔧 Разделители

### Пример 1: Разделитель `-F':'`

```
Исходная строка:
┌──────────────────────────────┐
│ John:25:Engineer:New York    │
└──────────────────────────────┘

С разделителем ':' → awk -F':'
┌──────┬────┬──────────┬──────────┐
│  $1  │ $2 │   $3     │   $4     │
│ John │ 25 │ Engineer │ New York │
└──────┴────┴──────────┴──────────┘
```

### Пример 2: Разделитель `-F'='`

```
Исходная строка:
┌────────────────────────────────────────────────────────────┐
│ custom_message_name=flag&custom_message_value=D&event=     │
└────────────────────────────────────────────────────────────┘

С разделителем '=' → awk -F'='
┌─────────────────────┬──────────────────────┬──────────────────┬───┐
│         $1          │         $2           │       $3         │$4 │
│ custom_message_name │ flag&custom_message_ │ D&custom_message │   │
│                     │       value          │     _event       │   │
└─────────────────────┴──────────────────────┴──────────────────┴───┘
                                                     ↑
                                              Нам нужно это!
```

---

## 🎓 Основные команды

```bash
┌────────────────────────────┬────────────────────────────────────┐
│ Команда                    │ Описание                           │
├────────────────────────────┼────────────────────────────────────┤
│ awk '{ print $1 }'         │ Вывести первое поле                │
│ awk '{ print $0 }'         │ Вывести всю строку                 │
│ awk '{ print $NF }'        │ Вывести последнее поле             │
│ awk -F':' '{ print $2 }'   │ Разделитель ':', вывести 2-е поле  │
│ awk 'NR > 1 { print }'     │ Пропустить первую строку           │
│ awk '/error/ { print }'    │ Вывести строки содержащие "error"  │
│ awk '$1 > 100 { print }'   │ Вывести если первое поле > 100     │
│ awk '{ print $1, $2 }'     │ Вывести 1-е и 2-е поля             │
│ awk '{ sum += $1 } END {   │ Посчитать сумму первой колонки     │
│   print sum }'             │                                    │
└────────────────────────────┴────────────────────────────────────┘
```

---

## 💡 Наш пример из задания

### Шаг 1: Первый AWK

```
Входная строка:
┌──────────────────────────────────────────────────────────┐
│ custom_message_name=flag&custom_message_value=D&event=   │
└──────────────────────────────────────────────────────────┘

Команда: awk -F'=' '{ print $3 }'
          ↓
Разбиваем по '=':
┌─────────────────────┬────────────────────────┬──────────────────┬───┐
│         $1          │          $2            │       $3         │$4 │
│ custom_message_name │ flag&custom_message... │ D&custom_mess... │   │
└─────────────────────┴────────────────────────┴──────────────────┴───┘
                                                       ↓
                                                  print $3
                                                       ↓
┌──────────────────────┐
│ D&custom_message_... │  ← Результат
└──────────────────────┘
```

### Шаг 2: Второй AWK

```
Входная строка (результат предыдущего шага):
┌────────────────────────────┐
│ D&custom_message_event     │
└────────────────────────────┘

Команда: awk -F'&' '{ print $1 }'
          ↓
Разбиваем по '&':
┌───┬──────────────────────┐
│$1 │         $2           │
│ D │ custom_message_event │
└───┴──────────────────────┘
  ↓
print $1
  ↓
┌───┐
│ D │  ← Итоговый результат!
└───┘
```

---

## 🔄 Полная визуализация цепочки

```
Исходные данные:
┌──────────────────────────────────────────────────────────────┐
│ custom_message_name=flag&custom_message_value=D&event=       │
│ custom_message_name=flag&custom_message_value=U&event=       │
│ custom_message_name=flag&custom_message_value=C&event=       │
│ custom_message_name=flag&custom_message_value=K&event=       │
└──────────────────────────────────────────────────────────────┘
                              ↓
         awk -F'=' '{ print $3 }' (берём 3-е поле после '=')
                              ↓
┌──────────────────────┐
│ D&custom_message_... │
│ U&custom_message_... │
│ C&custom_message_... │
│ K&custom_message_... │
└──────────────────────┘
                              ↓
         awk -F'&' '{ print $1 }' (берём 1-е поле после '&')
                              ↓
┌───┐
│ D │
│ U │
│ C │
│ K │
└───┘
    ↓
tr -d '\n' (склеиваем в одну строку)
    ↓
┌──────┐
│ DUCK │  ← Финальный результат!
└──────┘
```

---

## 📊 Специальные переменные

```
┌────────┬──────────────────────────────────────────────┐
│ $0     │ Вся строка целиком                           │
├────────┼──────────────────────────────────────────────┤
│ $1     │ Первое поле                                  │
├────────┼──────────────────────────────────────────────┤
│ $2     │ Второе поле                                  │
├────────┼──────────────────────────────────────────────┤
│ $3     │ Третье поле                                  │
├────────┼──────────────────────────────────────────────┤
│ $NF    │ Последнее поле (N = количество полей)        │
├────────┼──────────────────────────────────────────────┤
│ NF     │ Количество полей в текущей строке            │
├────────┼──────────────────────────────────────────────┤
│ NR     │ Номер текущей строки (Number of Record)     │
├────────┼──────────────────────────────────────────────┤
│ FS     │ Разделитель полей (Field Separator)          │
├────────┼──────────────────────────────────────────────┤
│ OFS    │ Разделитель вывода (Output Field Separator) │
└────────┴──────────────────────────────────────────────┘
```

---

## 🎮 Практические примеры

### Пример 1: Email → username

```
Входные данные:
┌─────────────────────┐
│ john@example.com    │
└─────────────────────┘
         ↓
awk -F'@' '{ print $1 }'
         ↓
┌──────┐
│ john │
└──────┘
```

### Пример 2: Путь файла → имя файла

```
Входные данные:
┌──────────────────────────┐
│ /home/user/document.txt  │
└──────────────────────────┘
         ↓
awk -F'/' '{ print $NF }'
         ↓
┌──────────────┐
│ document.txt │
└──────────────┘
```

### Пример 3: CSV → вторая колонка

```
Входные данные:
┌───────────────────────┐
│ John,25,Engineer      │
│ Alice,30,Designer     │
│ Bob,28,Developer      │
└───────────────────────┘
         ↓
awk -F',' '{ print $2 }'
         ↓
┌────┐
│ 25 │
│ 30 │
│ 28 │
└────┘
```

---

## 🚀 Продвинутые техники

### BEGIN и END

```
┌─────────────────────────────────────────┐
│ BEGIN { ... }  ← Выполняется ДО чтения  │
├─────────────────────────────────────────┤
│ { ... }        ← Для каждой строки      │
├─────────────────────────────────────────┤
│ END { ... }    ← Выполняется ПОСЛЕ      │
└─────────────────────────────────────────┘
```

### Пример: Подсчёт суммы

```
Входные данные:
┌────┐
│ 10 │
│ 20 │
│ 30 │
└────┘
   ↓
awk '{ sum += $1 } END { print sum }'
   ↓
┌────┐
│ 60 │
└────┘

Как это работает:
┌──────────────┬─────────┐
│ Строка       │ sum     │
├──────────────┼─────────┤
│ BEGIN        │ sum = 0 │
│ 10           │ sum = 10│
│ 20           │ sum = 30│
│ 30           │ sum = 60│
│ END          │ print 60│
└──────────────┴─────────┘
```

---

## 🎯 Шпаргалка по операторам

### Сравнения
```
┌──────────┬────────────────┐
│ ==       │ Равно          │
│ !=       │ Не равно       │
│ >        │ Больше         │
│ <        │ Меньше         │
│ >=       │ Больше/равно   │
│ <=       │ Меньше/равно   │
└──────────┴────────────────┘
```

### Логические
```
┌──────────┬────────────────┐
│ &&       │ И (AND)        │
│ ||       │ ИЛИ (OR)       │
│ !        │ НЕ (NOT)       │
└──────────┴────────────────┘
```

### Паттерны
```
┌──────────────────┬────────────────────────┐
│ /pattern/        │ Содержит pattern       │
│ $1 ~ /pattern/   │ Поле 1 содержит        │
│ $1 !~ /pattern/  │ Поле 1 НЕ содержит     │
└──────────────────┴────────────────────────┘
```

---

## 🎓 Заключение

AWK - это:
```
┌────────────────────────────────────────┐
│  ✅ Построчная обработка текста        │
│  ✅ Разбиение на поля по разделителю   │
│  ✅ Фильтрация и условия               │
│  ✅ Математические операции            │
│  ✅ Форматирование вывода              │
│  ✅ Встроен в любой Unix/Linux         │
└────────────────────────────────────────┘
```
